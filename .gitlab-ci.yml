stages:
  - tests

unit_and_integration_tests:
  image: docker/docker-latest
  stage: tests
  before_script:
    - pip install pipenv
    - pipenv install
  script:
    - pipenv run python -m pytest tests/